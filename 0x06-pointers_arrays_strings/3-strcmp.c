#include "main.h"
#include <string.h>
/**
 * _strcmp - compares two strings
 * @s1: the first string
 * @s2: the second string
 * Return: A positive num if s1 > s2
 *         A negative num if s2 > s1
 *         0 if s1 = s2
 */
int _strcmp(char *s1, char *s2)
{
	return (strcmp(s1, s2));
}
